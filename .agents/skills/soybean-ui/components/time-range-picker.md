# TimeRangePicker

Source URL: https://ui.soybeanjs.cn/components/time-range-picker
Markdown URL: https://ui.soybeanjs.cn/components/time-range-picker.md
Category: Forms
Description: TimeRangePicker lets users select a start and end time from a popup list while preserving the selected values as `TimeValue` objects.

## Overview

TimeRangePicker lets users select a start and end time from a popup list while preserving the selected values as `TimeValue` objects.

## Usage

Usage examples for time-range-picker are rendered on the site.

## Demo

Interactive demos for time-range-picker are rendered on the site.

## API

Structured API data is rendered on the site. Build-time API metadata was unavailable for this skill export.
