# 架构决策记录（ADR）索引

> 本目录存放已接受的架构决策记录（Architecture Decision Records）。ADR 用于固化「为什么这样设计」：当出现**不可逆或影响面大的技术决策**（分层、命名、许可、依赖方向等）时，新建一篇 ADR 记录背景、决策、权衡与后果。
>
> 治理规则见 [../GOVERNANCE.md](../GOVERNANCE.md)；导航入口见 [../README.md](../README.md)。

## 索引

| 编号                                        | 标题                                                        | 状态      | 日期       | 关联                                          |
| :------------------------------------------ | :---------------------------------------------------------- | :-------- | :--------- | :-------------------------------------------- |
| [0001](0001-peripheral-package-layering.md) | 外围包分层模型：拆解 headless-x，核心 headless 为唯一逻辑层 | ✅ 已接受 | 2026-08-13 | [ecosystem/README.md](../ecosystem/README.md) |

## 新建 ADR 流程

1. 确定决策影响面足够大（分层 / 命名 / 许可 / 依赖方向 / 不可逆发布决策）。
2. 复制下方模板，编号取**当前最大序号 + 1**（四位左对齐补零，如 `0002-`）。
3. 填写：状态（`proposed` → `accepted` / `rejected` / `superseded`）、背景、决策、权衡（含被拒方案）、后果、相关。
4. 在本索引表登记一行，并链接关联文档。
5. 提交信息：`docs(adr): add ADR-NNNN <标题>`。

## 模板

```markdown
# ADR-NNNN: <标题>

## 状态

proposed / accepted / rejected / superseded

## 背景

<为什么需要这个决策：现状、矛盾、触发因素>

## 决策

<我们决定怎么做，编号列出>

## 权衡

<对比被拒绝的方案；说明代价与理由>

## 后果

<决策落地后的影响、义务、不可逆点>

## 相关

- <关联文档链接>
```
