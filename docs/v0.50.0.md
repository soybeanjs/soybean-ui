# SoybeanUI v0.50.0 重构执行清单

> 定位：v0.50.0 版本的**原地重构执行清单**——依赖最小化、准入整改、日期与 Table/Form 引擎更换的**任务明细 + 状态跟踪**。给维护者与执行 Agent 看。
> 状态：🚧 执行中（任务清单版，2026-09-12 更新）。**品牌改名已拆出本清单**（⏸ 暂缓，独立按 [rebrand-vbean.md](./rebrand-vbean.md) 跟踪）。
> 基线：2026-09-12 · 当前版本 v0.40.0-beta.2 · 分支 `main`
> 决策来源：ubean-ui 重构实验仓（`/Users/soybean/Web/Projects/SoybeanJS/ubean-ui/docs`，migration / dependencies / architecture / aria-admission-remediation）。ubean-ui 是「fork 重建」实验；本方案是把其实验结论**落回本仓执行**。
> 组件规范：[`.agents/skills/soybean-ui-develop/`](../.agents/skills/soybean-ui-develop/SKILL.md)。准入规则正文以 skill `layers.md` 为准，本文只做**执行编排**。
> 状态图例：✅ 已完成（基线/验收通过） · 🚧 进行中 · ⬜ 待办 · ⏸ 暂缓（品牌改名拆出）

> **执行范围（2026-09-12）**：本清单执行**除品牌改名外的全部计划内容**。原方案曾与 VBean 品牌改名（SoybeanUI → VBean，逻辑层 `@soybeanjs/headless` → `@vbean/aria`）合并为同一次 breaking 发布；因改名工作量大、需单独编排，现拆出按 [rebrand-vbean.md](./rebrand-vbean.md) 独立跟踪。**本清单全部任务按现仓库命名施工**（`@soybeanjs/headless` / `packages/headless` / `@soybeanjs/ui` / `@soybeanjs/theme`），改名落地时路径与依赖名统一替换（见 [§2](#2-改名拆出本清单不执行)）。外围包 `ui-x` / `admin` / `chart` 已整包移除（v0.40.0 起），本文不再有「保留外围包分层」议题。

## 0. 结论先行（决策契约）

| 决策                | 选择                                                                                                                                                                                                                                                                                              | 状态         |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| 逻辑层改名          | **品牌级改名 `@soybeanjs/headless` → `@vbean/aria`**（目录 `packages/headless` → `packages/aria`）                                                                                                                                                                                                | ⏸ 拆出       |
| 品牌不变项          | UI 组件前缀仍为 **`S`**（定义为 **Styled**，样式封装层标识，不跟 ubean 的 `U`）；仓库生成器 `sui` 保留；`data-soybean-*` / `--soybean-*` / `sbean` CLI 属品牌级改名范围（→ `data-vbean-*` / `--vbean-*` / `vbean`），见 [rebrand-vbean.md](./rebrand-vbean.md)                                    | ⏸ 拆出       |
| 日期引擎            | **`date-fns` + `Date`**，移除 `@internationalized/date` / `@internationalized/number`；公开值为 `Date` / `{ date, time? }` / range `{ start, end }`；不做 CalendarDate 适配层                                                                                                                     | ⬜ 待办      |
| Table 引擎          | **`@tanstack/vue-table`**（列模型 / 排序 / 筛选 / 展开 / pin / size）；headless 只补 table/grid a11y、键盘、虚拟滚动（继续 `@tanstack/vue-virtual`）与 Compact；自研列模型退役                                                                                                                    | ⬜ 待办      |
| Form 引擎           | **`@tanstack/vue-form`**（字段级订阅、数组字段、Standard Schema）；自研 `useHeadlessForm` 退役；`@standard-schema/spec` 不再是直接依赖，由消费者的 Zod/Valibot 提供                                                                                                                               | ⬜ 待办      |
| dnd-kit             | **移除 `@dnd-kit/dom` / `@dnd-kit/vue`**；PageTabs 排序自研 `useSortableList`（pointer + 键盘横排排序）；多容器/虚拟拖拽不做                                                                                                                                                                      | ⬜ 待办      |
| auto-animate        | **移除 `@formkit/auto-animate`**；进出场一律 Vue `<Transition>` / UnoCSS 动画 preset；**同时修正 headless 层泄漏**（`form-field-base-compact` 中的 `v-auto-animate` 必须移到 UI 层）                                                                                                              | ⬜ 待办      |
| Drawer              | **升级为独立 aria 家族**（参考 [reka-ui Drawer](https://github.com/unovue/reka-ui/tree/main/packages/core/src/Drawer)，Vaul 方案：snap points / swipe dismiss / handle / nested）；现 `bottom-sheet` 家族的 `use-snap-points` / `use-scale-background` 为新家族基底，能力并入后 bottom-sheet 退役 | ⬜ 待办      |
| BottomSheet         | **退役**：snap/swipe/handle 由 Drawer 承接；Confirm/Cancel/Footer 变体由 UI 层组合 Drawer；不再维护平行家族                                                                                                                                                                                       | ⬜ 待办      |
| Card                | **改判 UI-only**（HAD-C4）：删除 headless `card` 家族，`SCard` 组合 `CollapsibleRoot`（`defaultOpen: true`）+ 自绘 chrome；`styles/card.ts` 配方与 `ui` 覆盖语义不变                                                                                                                              | ⬜ 待办      |
| 解剖壳              | Empty / Skeleton **下沉 UI-only**；List **删除家族**（纯 `ul/li` 走 HTML + UI class，交互列表走 listbox/tree）；Badge / Tag **UI-only**；Alert **补 `role="alert"` / `status` live 语义**                                                                                                         | ⬜ 待办      |
| NavigationMenu      | **v0.50.0 移除**（借改名+引擎大版本窗口提前）；唯一导航菜单家族为 `nav-menu`                                                                                                                                                                                                                      | ⬜ 待办      |
| Gate 2 补导出       | input-number（Clear）、layout（Placeholder）、menubar（Menus）、nav-menu（OptionCompact）、table（CompactHead/Row/VirtualSpacerRow）第一天起公开（文件已存在，仅缺 barrel 导出）                                                                                                                  | ⬜ 待办      |
| Popper/Arrow        | UI 层零样式转发（`packages/ui/src/components/{popper,arrow}`）**停止公开**；零样式原语统一从 `@soybeanjs/headless/popper` 取；公开样式面由 popover / tooltip / hover-card 承担                                                                                                                    | ⬜ 待办      |
| RovingFocus         | **已完成**：composable 形式（`useRovingFocusGroup` / `useRovingFocusGroupItem`），无 Group/Item 组件目录，本文仅登记                                                                                                                                                                              | ✅           |
| 浮层背景 a11y       | **已完成**：自研 `shared/mark-others.ts`（Base UI 方案：inert 优先 + WeakMap 引用计数 + aria-live 豁免 + `data-soybean-inert` 标记通道）+ `composables/use-hide-others.ts`，不依赖 `aria-hidden` 包                                                                                               | ✅           |
| `@soybeanjs/shared` | **消化私有包**：仅存 `src/vite.ts` 构建 helper，并入 `@vbean/scripts` 或各包内联；theme / unocss 不反向依赖 headless                                                                                                                                                                              | ⏸ 随改名暂缓 |
| 外围包              | **已整包移除**（v0.40.0 起）：ui-x（AI/chat 回核心 headless/ui，见 [ui-ai-roadmap.md](./ui-ai-roadmap.md)）、admin（shell 回核心，见 [ui-shell-roadmap.md](./ui-shell-roadmap.md)）、chart（charts 仅存文档站示例）；tree-shake 验收只落核心包（见 [§8](#8-tree-shake-验收核心包)）               | ✅           |
| 文档站              | **已完成**：apps/docs 已为 ubean SSG（`mode: 'ssg'`，content 集合 + en/zh 内置 i18n）                                                                                                                                                                                                             | ✅           |
| 消费者 CLI          | `sbean` → **`vbean`**（init/add/build/diff/mcp，品牌级改名）；仓库生成器 `sui` 保留，子命令跟随改名（catalog headless → aria）                                                                                                                                                                    | ⏸ 拆出       |

v0.50.0 是含 breaking change 的 minor（0.x 阶段允许）：不承诺 1:1 旧 API；日期值模型与 Table/Form API 必须提供迁移对照文档。旧 `@soybeanjs/*` 兼容仅由品牌级改名的墓碑版转发承接（见 [rebrand-vbean.md](./rebrand-vbean.md) §4），核心包不做兼容导出。

## 1. 基线：已吸收的 ubean 决策（✅ 已完成，无需施工）

以下 ubean 迁移方案中的「待自研 / 待更换」项，在本仓当前源码（v0.40.x）**已经落地**，v0.50.0 只做验证与登记，不重复施工：

| 项                                         | 现状证据                                                                                                                                                                                                                                                           | 状态 |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| `toContext` / `fromContext`                | [headless/shared/vue.ts](../packages/headless/src/shared/vue.ts) 已导出；headless 内 40+ 处使用，旧 `transformPropsToContext` 为 0；skill 与 AGENTS.md 已于 2026-09-10 同步                                                                                        | ✅   |
| `markOthers`（aria-hidden 替代）           | [headless/shared/mark-others.ts](../packages/headless/src/shared/mark-others.ts) + [use-hide-others.ts](../packages/headless/src/composables/use-hide-others.ts) 已落地：inert 优先（修正 Base UI #4678）、引用计数、aria-live 豁免、`data-soybean-inert` 标记通道 | ✅   |
| fuzzySearch（fuse.js 替代）                | [headless/shared/fuzzy.ts](../packages/headless/src/shared/fuzzy.ts) 已落地；[use-fuse.ts](../packages/headless/src/composables/use-fuse.ts) 包装为无依赖 fuzzy 评分；源码无 `fuse.js` 引用                                                                        | ✅   |
| 小工具自研（defu/klona/ohash/utils/hooks） | [headless/shared/object.ts](../packages/headless/src/shared/object.ts) 等已提供 `defu` / `klona` / `isEqual` / `keysOf`；源码无 `defu` / `klona` / `ohash` / `@soybeanjs/hooks` / `@soybeanjs/utils` 依赖（仅 `apps/nuxt/.output` 旧构建产物残留，重新构建即消）   | ✅   |
| RovingFocus composable 化                  | [use-roving-focus.ts](../packages/headless/src/composables/use-roving-focus.ts) 已存在；headless components 无 `roving-focus` 目录                                                                                                                                 | ✅   |
| ubean SSG 文档站                           | apps/docs 已运行 ubean 0.4.x（`mode: 'ssg'`），content 集合 `content/{en,zh}/`、`locales/{en,zh}.json`、`import.meta.glob` 示例发现                                                                                                                                | ✅   |
| vueuse 复用纪律                            | headless 直接依赖 `@vueuse/core`（`useDraggable` / `useResizeObserver` / `refAutoReset` 等），skill 已写明「不重造」                                                                                                                                               | ✅   |

## 2. 改名（⏸ 拆出，本清单不执行）

品牌级改名（`@soybeanjs/headless` → `@vbean/aria`、`data-soybean-*` → `data-vbean-*`、`sbean` → `vbean` 等）已**拆出本清单**，按 [rebrand-vbean.md](./rebrand-vbean.md) 独立编排与跟踪。本清单所有任务在改名落地前按现仓库命名施工；改名执行时：

- 全仓 import 路径替换 + `pnpm sui gen catalog headless` → `pnpm sui gen catalog aria` 重生成 constants / namespaced。
- docs content `content/{en,zh}/headless/` → `content/{en,zh}/aria/`；docs 菜单分组 `headless` → `aria`（[apps/docs/src/constants/menus.ts](../apps/docs/src/constants/menus.ts)）。
- 重生成 api / changelog / locale / schema / skills 并翻译；验收 `grep -r "@soybeanjs/headless" packages apps` 为 0。

## 3. 依赖最小化（⬜ 待办）

原则：**50–200 行能自研的不进运行时依赖；引擎级能力才保留第三方。** 小工具统一在 `@soybeanjs/headless` 的 `src/shared`（经 `@soybeanjs/headless/shared` 子路径导出）；theme / ui-uno 等不依赖 Vue 的纯包不引用 headless，所需 1–3 个纯函数各自内联。

### 3.1 决策总表

| 现依赖                              | 现状用途                                                     | 决策                                                                                | 落点                                                   | 状态 |
| ----------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ------------------------------------------------------ | ---- |
| `@dnd-kit/dom` / `@dnd-kit/vue`     | PageTabs 拖拽排序（2 个文件）                                | **移除**；自研 `useSortableList`（pointer + 键盘横排、占位尺寸、`aria-dropeffect`） | headless `shared` + composable                         | ⬜   |
| `@formkit/auto-animate`             | form-field 错误进出场（**在 headless 层，违规**）、tree 展开 | **移除**；Vue `<Transition>` / UnoCSS 动画 preset；headless 中的用法移到 UI 层      | UI 层                                                  | ⬜   |
| `@internationalized/date`           | 日期值模型（CalendarDate 等，12 个 date 文件 + 8 个组件族）  | **替换为 `date-fns` + `Date`**                                                      | headless `date/`（见 [§5](#5-日期引擎date-fns--date)） | ⬜   |
| `@internationalized/number`         | 数字格式（input-number）                                     | **替换为 `Intl.NumberFormat`**                                                      | headless shared / number family                        | ⬜   |
| `@standard-schema/spec`             | Form schema 类型                                             | **移除直接依赖**；随 TanStack Form 类型面，校验库由消费者 peer 提供                 | peer 可选                                              | ⬜   |
| 自研 Table 引擎（列模型/排序/筛选） | table 家族（20 文件 / 约 4100 行）                           | **更换为 `@tanstack/vue-table`**                                                    | headless table（见 [§6](#6-table--form-引擎tanstack)） | ⬜   |
| 自研 `useHeadlessForm`              | form 家族（含 shared klona 提交拷贝）                        | **更换为 `@tanstack/vue-form`**                                                     | headless form                                          | ⬜   |
| `@floating-ui/dom`                  | Popper 定位                                                  | **保留**                                                                            | headless 直接依赖                                      | ✅   |
| `@tanstack/vue-virtual`             | Virtualizer / Table 虚拟滚动                                 | **保留**                                                                            | headless 直接依赖                                      | ✅   |
| `embla-carousel`                    | Carousel                                                     | **保留**（carousel 子路径 peerOptional，见 §11-3）                                  | headless 直接依赖（建议 carousel 子路径 peerOptional） | ✅   |
| `@vueuse/core`                      | 通用 composable 兜底                                         | **保留为 peerOptional**；新增前先查                                                 | headless                                               | ✅   |
| `@soybeanjs/colord`                 | 颜色解析 / 主题派生                                          | **保留**（自有包，体积可控）                                                        | theme / headless color                                 | ✅   |
| `@soybeanjs/cva`                    | `cv` / `scv`                                                 | **保留**                                                                            | UI                                                     | ✅   |
| `@iconify/vue`                      | 图标                                                         | **保留为 UI 依赖**                                                                  | UI                                                     | ✅   |
| `@soybeanjs/shared`（私有包）       | 仅 `vite.ts` 构建 helper（theme/unocss vite.config）         | **消化**：并入 scripts 或各包内联，删除私有 workspace（随改名暂缓）                 | scripts / 各包内联                                     | ⏸    |
| —                                   | —                                                            | **新增** `date-fns`                                                                 | headless date                                          | ⬜   |
| —                                   | —                                                            | **新增** `@tanstack/vue-table`                                                      | headless table                                         | ⬜   |
| —                                   | —                                                            | **新增** `@tanstack/vue-form`                                                       | headless form                                          | ⬜   |

### 3.2 `@soybeanjs/headless` 运行时依赖目标白名单

- `vue`（peer）
- `@floating-ui/dom`
- `@tanstack/vue-virtual`
- `@tanstack/vue-table`、`@tanstack/vue-form`（table/form 引擎）
- `date-fns`
- `embla-carousel`（peerOptional 或接受主包携带但保证 tree-shake）
- `@vueuse/core`（peerOptional）
- `@soybeanjs/colord`（颜色家族）

`@soybeanjs/ui` 运行时只允许：`@soybeanjs/headless`（含 `/shared`、`/{component}`）、`@soybeanjs/theme`、`@soybeanjs/cva`、`@iconify/vue`。table/form/date/carousel 的重引擎不得在 UI 再依赖一遍。

### 3.3 任务明细

| #    | 任务                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 验收                                                                                             | 状态         |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------ |
| T3.1 | **dnd-kit 移除 → 自研 `useSortableList`**：新建 composable（pointer + 键盘横排、占位尺寸、`aria-dropeffect`）；[page-tabs-compact.vue](../packages/headless/src/components/page-tabs/page-tabs-compact.vue)（DragDropProvider/KeyboardSensor/PointerSensor/isSortable）与 [page-tabs-item.vue](../packages/headless/src/components/page-tabs/page-tabs-item.vue)（useSortable）改用之；[styles/page-tabs.ts](../packages/ui/src/styles/page-tabs.ts) 的 `.dnd-kit-preview_&` 类同步清理；删除两个 dnd-kit 依赖 | PageTabs 排序单测/e2e 对位；`@dnd-kit` 在 `packages/*/src` grep 为 0；package.json 依赖移除      | ⬜           |
| T3.2 | **auto-animate 移除 → Transition/UnoCSS**：修正 headless 泄漏——[form-field-base-compact.vue](../packages/headless/src/components/form/form-field-base-compact.vue) 的 `v-auto-animate` 移到 UI 层 `<Transition>`；[tree.vue](../packages/ui/src/components/tree/tree.vue) 与 [tree-virtualizer-animated.vue](../packages/ui/src/components/tree/tree-virtualizer-animated.vue) 展开动画改 Transition/UnoCSS 动画 preset；删除 [ui/test/setup.ts](../packages/ui/test/setup.ts) 的 auto-animate mock；移除依赖  | `@formkit/auto-animate` 在 `packages/*/src` grep 为 0；form 错误进出场 / tree 展开动画行为不回归 | ⬜           |
| T3.3 | **`@internationalized/date` + `@internationalized/number` 移除**：与 §5 日期引擎一并执行；[input-number/context.ts](../packages/headless/src/components/input-number/context.ts) 的 NumberFormatter/NumberParser 改 `Intl.NumberFormat`                                                                                                                                                                                                                                                                        | `@internationalized/` 在 `packages/*/src` grep 为 0（含 ui 测试）                                | ⬜           |
| T3.4 | **`@standard-schema/spec` 转 peer**：随 §6 Form 引擎一并执行（[form/index.ts](../packages/headless/src/components/form/index.ts) 的 `StandardSchemaV1` 导出面与 `form/core/*` 类型改由 TanStack Form 类型面承载）                                                                                                                                                                                                                                                                                              | `@standard-schema/spec` 从 headless 直接依赖移除，仅留类型引用                                   | ⬜           |
| T3.5 | **`@soybeanjs/shared` 消化**：仅存 [shared/src/vite.ts](../packages/shared/src/vite.ts) 构建 helper，并入 `scripts` 或 theme/unocss 各包内联，删除私有 workspace                                                                                                                                                                                                                                                                                                                                               | `@soybeanjs/shared` 依赖与 workspace 移除；theme/unocss 构建不受影响                             | ⏸ 随改名暂缓 |

### 3.4 验收

- `packages/headless/package.json` 不再出现 `@dnd-kit`、`@formkit/auto-animate`、`@internationalized/`、`@standard-schema/spec`。
- CI grep：`@dnd-kit`、`auto-animate`、`@internationalized/`、`fuse.js`、`defu`、`klona`、`ohash`、`aria-hidden` 在 `packages/*/src` 为 0。
- headless `src/shared` 纯函数模块（object/string/array/fuzzy/guard）单测覆盖；不出现 Vue composable / DOM 测量。
- bundle fixture：`import { SButton } from '@soybeanjs/ui'` 的构建产物不含 table / form / date-fns / embla 代码。

## 4. 准入整改（⬜ 待办）

准入规则正文（R1–R8 两段式 Gate 1/Gate 2）以 skill `layers.md` 为准；既有清单见 [headless-admission-remediation.md](./headless-admission-remediation.md)。本节编排**执行顺序与家族级定论**，每项完成后同步 `pnpm sui gen catalog headless` + `pnpm sui gen catalog ui` 重生成。

### 4.1 Drawer 独立 aria 家族 + BottomSheet 退役

现状：[UI SDrawer](../packages/ui/src/components/drawer/drawer.vue) 直接 compose `DialogCompact`；`bottom-sheet` 是完整 headless 家族，含 `use-snap-points.ts` / `use-scale-background.ts` / root-nested / Compact / Confirm / Cancel。

定论：snap points、swipe 进度、拖拽手柄、嵌套缩放是 Dialog 不具备的真实领域状态机，「Drawer = Dialog with a side」定位作废。

| #      | 任务                                                                                                                                                                                                                                                                                                                                                                                                                           | 验收                                                              | 状态 |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | ---- |
| T4.1.1 | 新建 `packages/headless/src/components/drawer/`：Root / Trigger / Portal / Overlay / Content / Handle / Close / Title / Description / SwipeArea / Viewport（+ 嵌套 Nested 钩子）                                                                                                                                                                                                                                               | 家族通过 Headless admission；目录结构完整                         | ⬜   |
| T4.1.2 | 以现 [bottom-sheet 的 use-snap-points.ts](../packages/headless/src/components/bottom-sheet/use-snap-points.ts) / [use-scale-background.ts](../packages/headless/src/components/bottom-sheet/use-scale-background.ts) 为复用基底，对照 reka-ui Drawer 补齐：swipe dismiss（轴锁定 / 方向阻尼 / 速度采样 / 滚动让位）、`v-model:snapPoint`、modal 三层级（`true` / `'trap-focus'` / `false`）；横向 side 首版声明「仅纵向 snap」 | drawer 单测覆盖 snap 吸附 / swipe 关闭 / scale background         | ⬜   |
| T4.1.3 | scale background / fadeFromIndex 作为 opt-in composable + prop 保留（bottom-sheet 行为合同转 Drawer 测试用例）                                                                                                                                                                                                                                                                                                                 | 行为合同不丢                                                      | ⬜   |
| T4.1.4 | `SDrawer` 从 compose Dialog 改为 compose Drawer 原语；`SBottomSheet` 退役，Confirm/Cancel/Footer 由 UI 层组合 Drawer 实现                                                                                                                                                                                                                                                                                                      | SDrawer 行为回归；SBottomSheet 相关 docs/示例/API 生成数据移除    | ⬜   |
| T4.1.5 | bottom-sheet 的 e2e / 单测行为合同全部在 drawer 有对位测试后删除 `bottom-sheet` 家族；`pnpm sui gen catalog headless` / `pnpm sui gen catalog ui` 重生成                                                                                                                                                                                                                                                                       | `grep bottom-sheet packages` 为 0（除迁移指引）；catalog 无 drift | ⬜   |

### 4.2 Card 改判 UI-only（HAD-C4）

| #      | 任务                                                                                                                                                                                                                  | 验收                                                       | 状态 |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ---- |
| T4.2.1 | 删除 `packages/headless/src/components/card/`（12 文件：card-compact / root / content / collapsible-trigger / header / title-root / title / footer / context / types / index）与 `@soybeanjs/headless/card` 子路径    | `@soybeanjs/headless/card` grep 为 0                       | ⬜   |
| T4.2.2 | `SCard` 重写：`CollapsibleRoot`（`defaultOpen: true`）+ 自绘 header/titleRoot/title/description/footer + `CollapsibleContent` / `CollapsibleTrigger`；`provideCollapsibleUi` 注入 recipe 的 root/content/trigger 子集 | SCard 渲染结构 + 可折叠行为回归（默认展开）                | ⬜   |
| T4.2.3 | [styles/card.ts](../packages/ui/src/styles/card.ts)、`CardUiSlot`、`ui` 覆盖语义不变；docs 与示例改引用；catalog/api 重生成                                                                                           | docs/示例无 headless card 引用；`sui gen catalog` 无 drift | ⬜   |

### 4.3 解剖壳与薄壳下沉

| #      | 家族       | 现状（headless 文件数）                                                           | 任务                                                                                                                                                                                       | 验收                                     | 状态 |
| ------ | ---------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- | ---- |
| T4.3.1 | `empty`    | 9 文件（root/media/content/header/title/description/compact/context/types/index） | 删除 headless 家族；`SEmpty` 做 UI-only（组合已有 Primitive / Icon）                                                                                                                       | `@soybeanjs/headless/empty` grep 为 0    | ⬜   |
| T4.3.2 | `list`     | 8 文件（root/item/title/description/content/context/types/index）                 | 删除家族；纯列表用 HTML + UI class；交互列表走 `listbox` / `tree`                                                                                                                          | 家族移除；无内部引用                     | ⬜   |
| T4.3.3 | `skeleton` | 3 文件（skeleton.vue/types/index）                                                | 下沉 UI-only（与 spinner 同类）                                                                                                                                                            | headless 无 skeleton 目录                | ⬜   |
| T4.3.4 | `badge`    | 6 文件（root/content/compact/context/types/index）                                | 下沉 UI-only；可关闭能力组合 Button/Icon                                                                                                                                                   | headless 无 badge 目录                   | ⬜   |
| T4.3.5 | `tag`      | 3 文件（tag.vue/types/index）                                                     | 下沉 UI-only；可关闭标签交互以 `tags-input` 为准；**顺手修正泄漏**：[tag.vue](../packages/headless/src/components/tag/tag.vue) 的 `defineOptions({ name: 'STag' })` 不得出现在 headless 层 | headless 无 tag 目录；无 `STag` 名称泄漏 | ⬜   |
| T4.3.6 | `alert`    | 8 文件（root/title/description/content/close/compact/context/types/index）        | **保留 headless 家族但补语义**：必须带 `role="alert"` / `status` 与关闭 `aria-label`（现 [alert-root.vue](../packages/headless/src/components/alert/alert-root.vue) 无 role）              | alert e2e 断言 role/status；axe 通过     | ⬜   |

### 4.4 NavigationMenu 移除

| #      | 任务                                                                                                                                                                                                                                                                                                                        | 验收                                                                     | 状态 |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---- |
| T4.4.1 | `navigation-menu` 家族（headless 13+ 文件 + [UI navigation-menu.vue](../packages/ui/src/components/navigation-menu/navigation-menu.vue) + `NavigationMenuOptionCompact`）v0.50.0 全链路移除：headless/UI 导出、[docs 双语页面](../apps/docs/src/content/en/ui/components/navigation-menu.md)、菜单 IA、catalog/api 生成数据 | `grep navigation-menu packages apps/docs/src/content` 为 0（除迁移指南） | ⬜   |
| T4.4.2 | 唯一导航菜单家族为 `nav-menu`；`NavMenuOptionCompact` 同步补公开导出（并入 T4.5.4）                                                                                                                                                                                                                                         | nav-menu 行为回归                                                        | ⬜   |
| T4.4.3 | 双语迁移指南保留一个版本周期（从 navigation-menu 文档改为「已移除，迁移到 nav-menu」指引）                                                                                                                                                                                                                                  | 迁移指南双语发布                                                         | ⬜   |

### 4.5 Gate 2 补导出（HAD-E）

现状核对（2026-09-12）：以下节点**文件均已存在，但家族 `index.ts` 未导出**，不进入 barrel 与 catalog：

| #      | 节点                                                             | 文件（已存在）                                                                                            | 任务                                                                                      | 状态 |
| ------ | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---- |
| T4.5.1 | `InputNumberClear`                                               | [input-number-clear.vue](../packages/headless/src/components/input-number/input-number-clear.vue)         | [input-number/index.ts](../packages/headless/src/components/input-number/index.ts) 补导出 | ⬜   |
| T4.5.2 | `LayoutPlaceholder`                                              | [layout-placeholder.vue](../packages/headless/src/components/layout/layout-placeholder.vue)               | [layout/index.ts](../packages/headless/src/components/layout/index.ts) 补导出             | ⬜   |
| T4.5.3 | `MenubarMenus`                                                   | [menubar-menus.vue](../packages/headless/src/components/menubar/menubar-menus.vue)                        | [menubar/index.ts](../packages/headless/src/components/menubar/index.ts) 补导出           | ⬜   |
| T4.5.4 | `NavMenuOptionCompact`                                           | [nav-menu-option-compact.vue](../packages/headless/src/components/nav-menu/nav-menu-option-compact.vue)   | [nav-menu/index.ts](../packages/headless/src/components/nav-menu/index.ts) 补导出         | ⬜   |
| T4.5.5 | `TableCompactHead` / `TableCompactRow` / `TableVirtualSpacerRow` | [table-compact-head.vue](../packages/headless/src/components/table/table-compact-head.vue) 等三个装配节点 | [table/index.ts](../packages/headless/src/components/table/index.ts) 补导出               | ⬜   |

公共验收：`pnpm sui gen catalog headless` 重生成 constants/components.ts + namespaced；catalog/api 无 drift；组件可从 `@soybeanjs/headless/{component}` 与 namespaced 导入。

### 4.6 Popper / Arrow UI 公开面收口

| #      | 任务                                                                                                                                                                                                                                                                           | 验收                                                            | 状态 |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- | ---- |
| T4.6.1 | 删除 [ui/arrow/index.ts](../packages/ui/src/components/arrow/index.ts)（零样式转发 `Arrow as SArrow`）与 [ui/popper](../packages/ui/src/components/popper/)（popper.vue + index.ts）的**公开导出**；零样式原语从 `@soybeanjs/headless/popper` / `@soybeanjs/headless/arrow` 取 | 无 `SPopper` / `SArrow` 内部引用                                | ⬜   |
| T4.6.2 | 公开样式面仅 popover / tooltip / hover-card；`styles/` 下不新增 popper.ts / arrow.ts；现 [styles/popper.ts](../packages/ui/src/styles/popper.ts) 按引用面收敛或删除                                                                                                            | docs 示例与内部引用无 S 前缀依赖；`sui gen catalog ui` 无 drift | ⬜   |

## 5. 日期引擎（date-fns + Date）（⬜ 待办）

现状：[headless/src/date/](../packages/headless/src/date/) 共 12 文件（calendar / comparators / formatter / parser / parts / placeholders / segment / types / use-date-field / utils / index），全部基于 `@internationalized/date`；8 个组件族引用（calendar 18 / calendar-range 17 / date-field 6 / date-picker 4 / date-range-field 6 / date-range-picker 4 / time-field 6 / time-range-field 6 文件）；ui 测试 [date.spec.ts](../packages/ui/test/specs/shared/date.spec.ts) 等 6 个 spec 直接构造 `CalendarDate` / `Time`。

| #    | 任务                                                                                                                                                                                    | 验收                                                                      | 状态 |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---- |
| T5.1 | **值模型**：日历日 `Date`（本地 00:00）或 `{ date: Date, time?: Date }`；range 为 `{ start: Date \| null, end: Date \| null }`；不暴露 `CalendarDate` / `ZonedDateTime`，不做长期适配层 | `date/types.ts` 无 CalendarDate 导出                                      | ⬜   |
| T5.2 | `date/` 内核重写：日历网格、segment、parse、format 全部基于 `date-fns` + `Intl.DateTimeFormat`；数字格式化走 `Intl.NumberFormat`（配合 T3.3）                                           | `@internationalized` 在 packages/src 与 ui 测试 grep 为 0；date/ 单测全绿 | ⬜   |
| T5.3 | 8 个组件族逐个迁移（Calendar / CalendarRange / DateField / DateRangeField / TimeField / TimeRangeField / DatePicker / DateRangePicker）；组件面与 props 保持兼容，仅值模型换型          | 8 族单测/e2e 对位；ui spec 改 `Date` 构造                                 | ⬜   |
| T5.4 | 第一期只做 local date；Zoned/时区需求单独立项，不偷偷加回 CalendarDate                                                                                                                  | 无时区专有 API 泄漏                                                       | ⬜   |
| T5.5 | **中英迁移对照文档**：`CalendarDate` → `Date` 值映射、format/parse 对照、range 写法对照，进 docs content（`content/{en,zh}/ui/...`）                                                    | 双语迁移文档随版本发布                                                    | ⬜   |

## 6. Table / Form 引擎（TanStack）（⬜ 待办）

**Table**

现状：自研列模型约 4100 行（[table/shared.ts](../packages/headless/src/components/table/shared.ts) 的 sortTableData / 筛选 / getTableAriaSort / getTableLeafColumns 等 + table-root/header/body/row/cell/content/scroll/compact 系列）。

| #    | 任务                                                                                                                                                                                                                                                     | 验收                                           | 状态 |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ---- |
| T6.1 | 引擎换 `@tanstack/vue-table`：column defs、sorting、filtering、expanding、column pinning/sizing 以 TanStack 命名为第一公民；旧 `TableColumn.dataIndex` / `sortState` 提供薄别名映射到 `accessorKey` / `SortingState`（仅高频映射，见 §11-4），不做兼容层 | 排序/筛选/展开/pin/size 行为合同单测全绿       | ⬜   |
| T6.2 | headless 层只保留：table/grid 角色、`aria-sort`、键盘导航、虚拟滚动（`@tanstack/vue-virtual`）、Compact 装配（TableCompact / CompactHead / CompactRow / VirtualSpacerRow）                                                                               | a11y 断言通过（axe）                           | ⬜   |
| T6.3 | UI 层：样式、筛选弹层、resize handle                                                                                                                                                                                                                     | STable 视觉回归                                | ⬜   |
| T6.4 | 服务端 `request`、查询工具栏、列管理面板不进核心第一期；Pro 能力以可选 composable 在 table 目录导出，保持 tree-shake，不建外围 table 包                                                                                                                  | tree-shake fixture 不含 table 代码（未引用时） | ⬜   |
| T6.5 | **迁移对照文档**：旧列模型 → TanStack 映射（dataIndex / sortState / 筛选用法）                                                                                                                                                                           | 双语迁移文档随版本发布                         | ⬜   |

**Form**

现状：自研 [form/core/use-form.ts](../packages/headless/src/components/form/core/use-form.ts)（`useHeadlessForm`）+ use-form-state / use-field-array / standard / shared / types，`@standard-schema/spec` 直接依赖。

| #     | 任务                                                                                                                                                                  | 验收                                                 | 状态 |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ---- |
| T6.6  | 引擎换 `@tanstack/vue-form`：字段级订阅、字段数组、提交生命周期；删除自研 `useHeadlessForm` 与 klona 提交拷贝路径                                                     | form 单测/e2e 行为合同全绿（校验/数组字段/提交状态） | ⬜   |
| T6.7  | headless 层只保留 `Form` / `Field` / `Label` / `Control` / `Description` / `Error` 的 id 接线与 `aria-invalid` / `aria-describedby`、Compact 装配                     | aria 断言通过（axe）                                 | ⬜   |
| T6.8  | Schema：消费者传入 Standard Schema 兼容对象（Zod/Valibot），库不运行时依赖 `@standard-schema/spec`（配合 T3.4）；错误进出场动画移到 UI 层 `<Transition>`（配合 T3.2） | `@standard-schema/spec` 直接依赖移除                 | ⬜   |
| T6.9  | Schema→UI 自动渲染不进 v0.50.0；若做，作为可选组件独立目录后议                                                                                                        | —                                                    | —    |
| T6.10 | **迁移对照文档**：`useHeadlessForm` → TanStack Form 用法对照                                                                                                          | 双语迁移文档随版本发布                               | ⬜   |

## 7. 分波交付与状态跟踪（主跟踪表）

每波独立验收；未完成当前波不开始下一波。波次执行状态以本表为准，沉淀结论回填本文与各路线图。**当前（2026-09-12）：全部任务 ⬜ 待办，无进行中项。**

| Wave | 任务组                                         | 任务号                          | 状态 | 验收要点                                                               |
| ---- | ---------------------------------------------- | ------------------------------- | ---- | ---------------------------------------------------------------------- |
| —    | 基线（§1）                                     | —                               | ✅   | 已落地，仅验证与登记                                                   |
| W0   | 改名（[rebrand-vbean.md](./rebrand-vbean.md)） | —                               | ⏸    | 拆出本清单，独立编排                                                   |
| W1   | 依赖清理                                       | T3.1–T3.2（T3.3/T3.4 随 W3/W4） | ⬜   | 依赖白名单 grep 为 0；PageTabs 排序单测/e2e 对位；form/tree 动画不回归 |
| W2   | 准入整改                                       | T4.1.x–T4.6.x                   | ⬜   | 各家族验收见 §4；准入清单全部关闭                                      |
| W3   | 日期                                           | T3.3、T5.1–T5.5                 | ⬜   | 日期单测/e2e 对位；`@internationalized` grep 为 0；双语迁移文档        |
| W4   | Table / Form                                   | T3.4、T6.1–T6.10                | ⬜   | table/form 行为合同单测全绿；`@standard-schema` 直接依赖移除           |
| W5   | Tree-shake 验收                                | §8 全部                         | ⬜   | fixture 产物断言通过；CI 上线                                          |

## 8. Tree-shake 验收（核心包）

**外围包现状：`ui-x` / `admin` / `chart` 已整包移除**（v0.40.0 起）：

- `ui-x`：AI/chat 组件回核心 headless/ui（见 [ui-ai-roadmap.md](./ui-ai-roadmap.md)）；`markstream-vue` 2.x 改为 `@soybeanjs/ui` 的**直接依赖**（仅 UI 包，不进 headless 层），`shiki` / `mermaid` / `katex` 为可选 peer，缺失时优雅降级。
- `admin`：shell 组件回核心（见 [ui-shell-roadmap.md](./ui-shell-roadmap.md)，headless `shell/` + `SLayoutShell` / `SPageHeader` 等）。
- `chart`：charts 仅存文档站示例（[TanStack Charts](https://tanstack.com/charts)，`apps/docs/src/examples/chart/`），不进核心依赖树。

v0.50.0 的 tree-shake 要求全部落在**核心包**（[headless/package.json](../packages/headless/package.json) 已 `sideEffects: false`）：

| #    | 任务                                                                                                                                                                                | 验收                                       | 状态 |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ---- |
| T8.1 | `@soybeanjs/headless` / `@soybeanjs/ui` 的 `exports` 含 `.` 与 `./{component}` 子路径                                                                                               | exports 检查通过                           | ⬜   |
| T8.2 | `sideEffects` 精确到 CSS / UnoCSS include；barrel 无副作用初始化；组件 SFC 无模块级 `window` 监听                                                                                   | sideEffects 扫描通过                       | ⬜   |
| T8.3 | bundle fixture（W5）：`import { SButton } from '@soybeanjs/ui'` 产物不含 table / form / date-fns / embla 代码（markstream 属 UI 可选渲染底座，仅经 `SMarkdown` 显式引用时进入产物） | fixture 产物断言通过（vitest/rollup 分析） | ⬜   |
| T8.4 | AI/chat 的 `markstream-vue` 不得进入 headless 层；headless 运行时依赖遵守 §3.2 白名单                                                                                               | 白名单 grep 门禁（CI）                     | ⬜   |
| T8.5 | CI 上线 grep 门禁：§3.4 黑名单包名在 `packages/*/src` 为 0                                                                                                                          | `ci.yml` 增加检查步骤                      | ⬜   |

## 9. 风险

| 风险                                      | 缓解                                                                                                |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 引擎更换同版本，升级路径陡                | 0.x 阶段明确不提供兼容层；日期/Table/Form 三份中英迁移对照文档随版本发布；changelog 逐项列 breaking |
| date-fns 时区能力弱于 internationalized   | 第一期只做 local date；Zoned 需求单独立项                                                           |
| TanStack Table/Form 心智与旧自研 API 不同 | 文档以 TanStack 为准；提供 Compact 示例而非兼容层；薄别名仅限最常用映射                             |
| Drawer 重写期间 bottom-sheet 行为缺口     | bottom-sheet 既有单测/e2e 行为合同先转 drawer 测试再删家族；scale background / Confirm-Cancel 不丢  |
| auto-animate 移除后动画手感变化           | 用 UnoCSS 动画 preset 与 `<Transition>` 对位原行为；视觉回归走 e2e 截图                             |
| 品牌改名拆出后同版本发布窗口压力          | 改名按 [rebrand-vbean.md](./rebrand-vbean.md) 独立编排，二者可不同版本落地                          |

## 10. 明确不做

- 核心包对旧 `@soybeanjs/*` 的兼容导出 / 别名 re-export（旧包兼容仅由品牌级改名的墓碑版转发承接，见 [rebrand-vbean.md](./rebrand-vbean.md) §4）。
- `CalendarDate` 适配层长期保留。
- 旧自研 Table/Form API 的兼容层。
- 重建 `headless-x`；把 AG Grid / Formily 设计器做进核心。
- 为每个已发布组件承诺 1:1 API 不变。
- 多容器 / 虚拟列表拖拽（dnd 完整能力）——出现真实需求再单独立项。

## 11. 待决策项（2026-09-12 已敲定）

原「待决策」项已按建议敲定并落入上述任务清单：

| #   | 议题                         | 决策（2026-09-12 敲定）                                                      | 落点        |
| --- | ---------------------------- | ---------------------------------------------------------------------------- | ----------- |
| 1   | NavigationMenu 移除时机      | **v0.50.0 移除**：引擎大版本窗口内，deprecated 包袱不跨版本                  | T4.4.x      |
| 2   | `@soybeanjs/shared` 消化方式 | **并入 scripts**：构建 helper 集中，纯包保持零内部依赖（随改名 W0 暂缓执行） | T3.5（⏸）   |
| 3   | embla-carousel 依赖形态      | **carousel 子路径 peerOptional**：与「重引擎不进主包」白名单原则一致         | §3.2 白名单 |
| 4   | 旧 Table/Form 薄别名范围     | **仅高频薄别名**（`dataIndex` / `sortState`）+ 迁移文档，避免半兼容层        | T6.1 / T6.5 |
