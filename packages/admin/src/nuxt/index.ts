// Nuxt module for @soybeanjs/admin.
// TODO(admin): scaffold placeholder — implement Nuxt auto-registration once components exist.

import { defineNuxtModule } from '@nuxt/kit';

export default defineNuxtModule({
  meta: {
    name: '@soybeanjs/admin',
    configKey: 'admin'
  },
  setup() {
    // TODO(admin): register resolver / transpile.
  }
});
