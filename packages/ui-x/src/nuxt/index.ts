// Nuxt module for @soybeanjs/ui-x.
// TODO(ui-x): scaffold placeholder — implement Nuxt auto-registration once components exist.

import { defineNuxtModule } from '@nuxt/kit';

export default defineNuxtModule({
  meta: {
    name: '@soybeanjs/ui-x',
    configKey: 'ui-x'
  },
  setup() {
    // TODO(ui-x): register resolver / transpile.
  }
});
