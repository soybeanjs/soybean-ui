import {
  TooltipProvider as STooltipProvider,
  TooltipRoot as STooltipRoot,
  TooltipTrigger as STooltipTrigger
} from '@soybean-ui/primitives';
import STooltip from './source/tooltip.vue';
import STooltipArrow from './source/tooltip-arrow.vue';
import STooltipContent from './source/tooltip-content.vue';

export { STooltip, STooltipArrow, STooltipContent, STooltipProvider, STooltipRoot, STooltipTrigger };

export * from './types';
