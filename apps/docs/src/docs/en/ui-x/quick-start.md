# Quick Start

> TODO(ui-x): scaffold placeholder — fill quick start guide.

Once installed, register components via the resolver:

```ts
// vite.config.ts
import { uiXResolver } from '@soybeanjs/ui-x/resolver';
```
