# Quick Start

> TODO(chart): scaffold placeholder — fill quick start guide.

Once installed, register components via the resolver:

```ts
// vite.config.ts
import { chartResolver } from '@soybeanjs/chart/resolver';
```
