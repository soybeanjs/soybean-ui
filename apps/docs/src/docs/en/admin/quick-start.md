# Quick Start

> TODO(admin): scaffold placeholder — fill quick start guide.

Once installed, register components via the resolver:

```ts
// vite.config.ts
import { adminResolver } from '@soybeanjs/admin/resolver';
```
