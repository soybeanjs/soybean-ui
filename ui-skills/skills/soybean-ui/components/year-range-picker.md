# YearRangePicker

Source URL: https://ui.soybeanjs.cn/components/year-range-picker
Markdown URL: https://ui.soybeanjs.cn/components/year-range-picker.md
Category: Forms
Description: YearRangePicker lets users select a start and end year from a paged year grid while preserving the selected values as `DateValue` objects anchored to the first day of each year.

## Overview

YearRangePicker lets users select a start and end year from a paged year grid while preserving the selected values as `DateValue` objects anchored to the first day of each year.

## Usage

Usage examples for year-range-picker are rendered on the site.

## Demo

Interactive demos for year-range-picker are rendered on the site.

## API

Structured API data is rendered on the site. Build-time API metadata was unavailable for this skill export.
